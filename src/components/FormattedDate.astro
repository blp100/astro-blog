---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="text-zinc-400 dark:text-zinc-500" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
